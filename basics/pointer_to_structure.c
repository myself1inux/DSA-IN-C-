#include<stdio.h>
int main(){
    struct rectangle {
        int length;
        int breadth;
    };
    struct rectangle r1;
    struct rectangle *p; //Here we are creating a pointer which stores the address of a structure named rectangle
    


    return 0;
}